import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  basePath: '/bifrost/model-library',
  assetPrefix: '/bifrost/model-library',
};

export default nextConfig;
